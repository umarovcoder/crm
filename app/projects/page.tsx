"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { createClient } from "@supabase/supabase-js";

const sb = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!);

type Project = { id:number; name:string; description:string|null; status:string; created_at:string; updated_at:string };
type Task = { id:number; title:string; status:string; priority:string; project_id:number|null; due_date:string|null; assignee:string|null };

const statuses = ["Active","Archived"];
const taskStatuses = ["To Do","In Progress","Review","Done"];

export default function Projects(){
  const [projects,setProjects]=useState<Project[]>([]),[tasks,setTasks]=useState<Task[]>([]),[loading,setLoading]=useState(true);
  const [modal,setModal]=useState(false),[editing,setEditing]=useState<Project|null>(null),[name,setName]=useState(""),[description,setDescription]=useState(""),[status,setStatus]=useState("Active"),[q,setQ]=useState(""),[selected,setSelected]=useState<Project|null>(null),[saving,setSaving]=useState(false),[toast,setToast]=useState("");
  const note=(s:string)=>{setToast(s);setTimeout(()=>setToast(""),2200)};
  const load=async()=>{setLoading(true);const [p,t]=await Promise.all([sb.from("projects").select("*").order("id"),sb.from("tasks").select("id,title,status,priority,project_id,due_date,assignee").order("id",{ascending:false})]);if(p.error)note(p.error.message);else setProjects((p.data||[]) as Project[]);if(!t.error)setTasks((t.data||[]) as Task[]);setLoading(false)};
  useEffect(()=>{load()},[]);
  const filtered=useMemo(()=>projects.filter(p=>!q||`${p.name} ${p.description||""}`.toLowerCase().includes(q.toLowerCase())),[projects,q]);
  const stats=(p:Project)=>{const x=tasks.filter(t=>t.project_id===p.id);return {total:x.length,done:x.filter(t=>t.status==="Done").length,open:x.filter(t=>t.status!=="Done").length}};
  const openNew=()=>{setEditing(null);setName("");setDescription("");setStatus("Active");setModal(true)};
  const openEdit=(p:Project)=>{setEditing(p);setName(p.name);setDescription(p.description||"");setStatus(p.status);setModal(true)};
  const save=async(e:React.FormEvent)=>{e.preventDefault();if(!name.trim())return;setSaving(true);const payload={name:name.trim(),description:description.trim()||null,status,updated_at:new Date().toISOString()};const r=editing?await sb.from("projects").update(payload).eq("id",editing.id).select().single():await sb.from("projects").insert(payload).select().single();if(r.error)note(r.error.message);else{if(editing)setProjects(x=>x.map(p=>p.id===editing.id?r.data as Project:p));else setProjects(x=>[...x,r.data as Project]);setModal(false);note(editing?"Project yangilandi":"Project yaratildi")}setSaving(false)};
  const archive=async(p:Project)=>{const next=p.status==="Archived"?"Active":"Archived";const r=await sb.from("projects").update({status:next,updated_at:new Date().toISOString()}).eq("id",p.id).select().single();if(r.error)note(r.error.message);else{setProjects(x=>x.map(a=>a.id===p.id?r.data as Project:a));if(selected?.id===p.id)setSelected(r.data as Project);note(next==="Archived"?"Project arxivlandi":"Project qayta aktiv qilindi")}};
  if(loading)return <main className="load">Projects yuklanmoqda...</main>;
  const selectedTasks=selected?tasks.filter(t=>t.project_id===selected.id):[];
  return <main><header><b>Latta Putta <small>CRM</small></b><nav><Link href="/">Dashboard / Sdelki</Link><Link href="/tasks">Tasks</Link><Link className="active" href="/projects">Projects</Link></nav><button className="primary" onClick={openNew}>＋ Yangi project</button></header>
  <section className="page"><div className="hero"><div><h1>Projects</h1><p>Jamoa ishlarini projectlar bo‘yicha tartiblang.</p></div></div><div className="toolbar"><input placeholder="🔎 Project qidirish..." value={q} onChange={e=>setQ(e.target.value)}/><span>{filtered.length} project</span></div>
  <div className="grid">{filtered.map(p=>{const s=stats(p);const pct=s.total?Math.round(s.done/s.total*100):0;return <article className={`project ${p.status==="Archived"?"archived":""}`} key={p.id} onClick={()=>setSelected(p)}><div className="ptop"><span className="dot">●</span><button onClick={e=>{e.stopPropagation();openEdit(p)}}>⋯</button></div><h2>{p.name}</h2><p>{p.description||"Tavsif qo‘shilmagan"}</p><div className="meta"><span>{p.status}</span><span>{s.total} task</span></div><div className="progress"><i style={{width:`${pct}%`}}/></div><footer><b>{pct}%</b><span>{s.done} / {s.total} done</span></footer></article>})}<button className="newcard" onClick={openNew}>＋<b>Yangi project</b><span>Team uchun yangi workspace</span></button></div></section>
  {selected&&<div className="overlay"><div className="detail"><div className="dh"><div><small>PROJECT</small><h2>{selected.name}</h2><p>{selected.description||"Tavsif yo‘q"}</p></div><div className="actions"><button onClick={()=>openEdit(selected)}>Tahrirlash</button><button onClick={()=>archive(selected)}>{selected.status==="Archived"?"Aktivlashtirish":"Archive"}</button><button onClick={()=>setSelected(null)}>×</button></div></div><div className="stats"><b>{selectedTasks.length}<small>Total</small></b><b>{selectedTasks.filter(t=>t.status==="Done").length}<small>Done</small></b><b>{selectedTasks.filter(t=>t.status!=="Done").length}<small>Open</small></b></div><div className="tasklist"><div className="th"><b>Task</b><b>Status</b><b>Priority</b><b>Deadline</b></div>{selectedTasks.length?selectedTasks.map(t=><div className="tr" key={t.id}><span>#{t.id} {t.title}</span><span>{t.status}</span><span>{t.priority}</span><span>{t.due_date||"—"}</span></div>):<div className="empty">Bu projectda hali task yo‘q.</div>}</div></div></div>}
  {modal&&<div className="overlay"><form className="modal" onSubmit={save}><div className="mh"><h2>{editing?"Projectni tahrirlash":"Yangi project"}</h2><button type="button" onClick={()=>setModal(false)}>×</button></div><label>Project nomi<input required autoFocus value={name} onChange={e=>setName(e.target.value)}/></label><label>Tavsif<textarea rows={4} value={description} onChange={e=>setDescription(e.target.value)}/></label><label>Status<select value={status} onChange={e=>setStatus(e.target.value)}>{statuses.map(x=><option key={x}>{x}</option>)}</select></label><div className="actions bottom"><button type="button" onClick={()=>setModal(false)}>Bekor qilish</button><button className="primary" disabled={saving}>{saving?"Saqlanmoqda...":"Saqlash"}</button></div></form></div>}
  {toast&&<div className="toast">✓ {toast}</div>}<style jsx>{css}</style></main>
}

const css=`*{box-sizing:border-box}body{margin:0}main{min-height:100vh;background:#f1f2f4;color:#172033;font-family:Arial,sans-serif}header{height:64px;background:#172b4d;color:#fff;display:flex;align-items:center;padding:0 24px;gap:28px}header b{font-size:18px}header small{color:#aebbd0}nav{display:flex;gap:8px;flex:1}nav a{color:#d7e0ee;text-decoration:none;padding:9px 12px;border-radius:7px;font-weight:700}nav a.active,nav a:hover{background:#ffffff1a;color:#fff}.primary{border:0;background:#0c66e4;color:#fff;border-radius:8px;padding:10px 15px;font-weight:700;cursor:pointer}.page{padding:25px 24px}.hero{margin-bottom:18px}.hero h1{margin:0 0 6px;font-size:27px}.hero p{margin:0;color:#6b7280}.toolbar{display:flex;gap:12px;align-items:center;margin-bottom:18px}.toolbar input{flex:1;max-width:650px;height:40px;border:1px solid #dfe1e6;border-radius:8px;padding:0 12px;background:#fff}.toolbar span{font-size:12px;color:#6b7280}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(270px,1fr));gap:14px}.project,.newcard{min-height:210px;border:1px solid #dfe1e6;border-radius:11px;background:#fff;padding:17px;text-align:left}.project{cursor:pointer}.project:hover{border-color:#b6c2d2;box-shadow:0 5px 18px #00000010}.project.archived{opacity:.65}.ptop{display:flex;justify-content:space-between}.dot{color:#0c66e4}.ptop button{border:0;background:none;font-size:20px;color:#6b7280;cursor:pointer}.project h2{font-size:18px;margin:16px 0 7px}.project p{font-size:12px;color:#6b7280;min-height:34px}.meta{display:flex;justify-content:space-between;font-size:11px;color:#6b7280}.progress{height:7px;background:#edf0f2;border-radius:9px;margin:18px 0 7px;overflow:hidden}.progress i{display:block;height:100%;background:#0c66e4;border-radius:9px}.project footer{display:flex;justify-content:space-between;font-size:11px;color:#6b7280}.project footer b{color:#172033}.newcard{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:7px;color:#6b7280;cursor:pointer;border-style:dashed}.newcard:first-line{font-size:25px}.newcard b{color:#172033}.overlay{position:fixed;inset:0;background:#091e4277;display:grid;place-items:center;padding:20px;z-index:10}.detail,.modal{width:min(900px,100%);max-height:90vh;overflow:auto;background:#fff;border-radius:12px;box-shadow:0 20px 60px #0004}.dh,.mh{display:flex;justify-content:space-between;gap:20px;padding:20px;border-bottom:1px solid #edf0f2}.dh h2,.mh h2{margin:5px 0;font-size:21px}.dh p{color:#6b7280}.actions{display:flex;gap:7px;align-items:flex-start}.actions button,.bottom button{border:1px solid #dfe1e6;background:#fff;border-radius:7px;padding:9px 13px;font-weight:700;cursor:pointer}.stats{display:flex;gap:35px;padding:18px 20px;border-bottom:1px solid #edf0f2}.stats b{font-size:22px}.stats small{display:block;font-size:10px;color:#8993a4;font-weight:400}.tasklist{padding:10px 20px 20px}.th,.tr{display:grid;grid-template-columns:2fr 1fr .8fr 1fr;gap:12px;padding:11px;border-bottom:1px solid #edf0f2;font-size:12px}.th{color:#8993a4;font-size:10px;text-transform:uppercase}.empty{padding:30px;text-align:center;color:#8993a4}.modal{width:min(600px,100%)}.modal>label{display:grid;gap:6px;margin:16px 20px;font-size:12px;font-weight:700;color:#4b5563}.modal input,.modal textarea,.modal select{border:1px solid #dfe1e6;border-radius:7px;padding:10px;font:inherit;background:#fff}.bottom{justify-content:flex-end;border-top:1px solid #edf0f2;padding:15px 20px}.toast{position:fixed;right:22px;bottom:22px;background:#172b4d;color:#fff;padding:12px 17px;border-radius:8px;z-index:20}.load{display:grid;place-items:center;min-height:100vh}@media(max-width:700px){header{padding:0 12px;gap:10px}nav a:first-child{display:none}.page{padding:18px 12px}.grid{grid-template-columns:1fr}.th,.tr{grid-template-columns:1.7fr 1fr}.th b:nth-child(n+3),.tr span:nth-child(n+3){display:none}.dh{flex-direction:column}.actions{flex-wrap:wrap}}`;